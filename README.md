# project-1
This project is a simple and dynamic web application built using HTML, CSS, and JavaScript that demonstrates how to create and manage a product listing using a JSON file as a data source.  The application fetches product data from a local JSON file and dynamically renders it on the webpage using JavaScript.
